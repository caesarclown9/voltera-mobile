# ============================================================================
# EvPower Mobile App - Environment Configuration Template
# ============================================================================
#
# ВАЖНО: Этот файл НЕ содержит настоящих ключей!
#
# Инструкция по настройке:
# 1. Скопируйте этот файл: cp .env.example .env
# 2. Замените все значения на реальные ключи
# 3. НИКОГДА не коммитьте .env в git!
#
# ============================================================================

# ----------------------------------------------------------------------------
# Backend API Configuration
# ----------------------------------------------------------------------------
# URL вашего OCPP backend сервера
VITE_API_URL=https://ocpp.evpower.kg

# WebSocket URL для real-time обновлений
VITE_WEBSOCKET_URL=wss://ocpp.evpower.kg

# Использовать реальный API (true) или mock (false)
VITE_USE_REAL_API=true

# ----------------------------------------------------------------------------
# Supabase Configuration
# ----------------------------------------------------------------------------
# Получить на: https://app.supabase.com/project/_/settings/api
#
# ВАЖНО:
# - Используйте ANON key (публичный ключ с RLS защитой)
# - НЕ используйте SERVICE_ROLE key на клиенте!
# - Для production создайте отдельные ключи с ограничениями

VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ----------------------------------------------------------------------------
# Yandex Maps API
# ----------------------------------------------------------------------------
# Получить на: https://developer.tech.yandex.ru/
#
# РЕКОМЕНДУЕТСЯ: Создать restricted key с ограничениями:
# - Только домены: evpower.kg, *.evpower.kg
# - Только API: JavaScript API и Геокодер

VITE_YANDEX_MAPS_API_KEY=your-yandex-maps-api-key-here

# ----------------------------------------------------------------------------
# Feature Flags
# ----------------------------------------------------------------------------
# Включить/выключить функциональность без перекомпиляции

# Push уведомления (требует Firebase setup на mobile)
VITE_ENABLE_PUSH_NOTIFICATIONS=true

# Аналитика (Google Analytics, Amplitude, etc)
VITE_ENABLE_ANALYTICS=false

# Debug режим (дополнительное логирование)
VITE_DEBUG_MODE=false

# Показывать devtools на production (только для testing)
VITE_SHOW_DEVTOOLS=false

# ----------------------------------------------------------------------------
# Mobile App Configuration (optional)
# ----------------------------------------------------------------------------
# Версия приложения (синхронизируется с package.json)
VITE_APP_VERSION=1.0.1

# Bundle ID (для глубоких ссылок)
VITE_APP_BUNDLE_ID=kg.evpower.app

# Deep link scheme
VITE_APP_URL_SCHEME=evpower

# ----------------------------------------------------------------------------
# Development Only
# ----------------------------------------------------------------------------
# Эти переменные только для development, не нужны в production

# Mock data для тестирования без backend
# VITE_USE_MOCK_DATA=false

# Задержка для mock API (мс)
# VITE_MOCK_API_DELAY=500

# Показать границы компонентов для отладки layout
# VITE_DEBUG_LAYOUT=false

# ----------------------------------------------------------------------------
# SECURITY CHECKLIST
# ----------------------------------------------------------------------------
# Перед деплоем убедитесь:
#
# □ .env файл НЕ закоммичен в git
# □ .gitignore содержит .env
# □ Все ключи ротированы (новые, не из примеров)
# □ Yandex Maps ключ с domain restrictions
# □ Supabase RLS policies настроены
# □ VITE_DEBUG_MODE=false для production
# □ Секреты храняться в CI/CD (GitHub Secrets, не в коде)
#
# ============================================================================
