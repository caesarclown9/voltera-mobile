<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- AppTheme.NoActionBar with display cutout support for Android 8.1+ (API 27+) -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!-- Enable drawing behind system bars -->
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <!-- Use dark status bar icons (for light background) -->
        <item name="android:windowLightStatusBar">true</item>
        <!-- Display cutout mode for devices with notches (Android 9+) -->
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <!-- Support for edge-to-edge and large screens -->
        <item name="android:enforceNavigationBarContrast">false</item>
        <item name="android:enforceStatusBarContrast">false</item>
    </style>
</resources>
