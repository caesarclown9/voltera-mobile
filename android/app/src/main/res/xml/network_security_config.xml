<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Certificate Pinning Configuration -->

    <!-- Production API -->
    <domain-config>
        <domain includeSubdomains="false">ocpp.evpower.kg</domain>
        <pin-set expiration="2026-01-01">
            <!-- Current certificate hash for ocpp.evpower.kg -->
            <!-- Generated: 2025-10-21 -->
            <pin digest="SHA-256">oZb2ItbSoJl3Kamv2sgIeC345I3lhH5V7HblBOPDPUs=</pin>
            <!-- Backup pin: Let's Encrypt R12 issuer -->
            <pin digest="SHA-256">SbqmW+BAJEQrrUnIU4uVF0v8P+uz0K3GpCQu2cl/AUo=</pin>
        </pin-set>
        <trustkit-config>
            <report-uri>https://api.evpower.kg/security/pinning-report</report-uri>
            <enforce-pinning>true</enforce-pinning>
        </trustkit-config>
    </domain-config>

    <!-- Debug configuration (only in debug builds) -->
    <debug-overrides>
        <trust-anchors>
            <!-- Trust user added CAs for debugging with proxies like Charles -->
            <certificates src="user" />
            <certificates src="system" />
        </trust-anchors>
    </debug-overrides>

    <!-- Default configuration for other domains -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>