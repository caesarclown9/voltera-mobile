<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Certificate Pinning Configuration -->

    <!-- Production API -->
    <domain-config>
        <domain includeSubdomains="false">ocpp.evpower.kg</domain>
        <pin-set expiration="2026-01-01">
            <!-- Replace with actual certificate SHA256 hashes -->
            <!-- Get hash: openssl s_client -connect ocpp.evpower.kg:443 | openssl x509 -pubkey -noout | openssl pkey -pubin -outform der | openssl dgst -sha256 -binary | openssl enc -base64 -->
            <pin digest="SHA-256">AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=</pin>
            <!-- Backup pin -->
            <pin digest="SHA-256">BBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBB=</pin>
        </pin-set>
        <trustkit-config>
            <report-uri>https://api.evpower.kg/security/pinning-report</report-uri>
            <enforce-pinning>true</enforce-pinning>
        </trustkit-config>
    </domain-config>

    <!-- Main API -->
    <domain-config>
        <domain includeSubdomains="true">api.evpower.kg</domain>
        <pin-set expiration="2026-01-01">
            <pin digest="SHA-256">CCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCC=</pin>
        </pin-set>
    </domain-config>

    <!-- Debug configuration (only in debug builds) -->
    <debug-overrides>
        <trust-anchors>
            <!-- Trust user added CAs for debugging with proxies like Charles -->
            <certificates src="user" />
            <certificates src="system" />
        </trust-anchors>
    </debug-overrides>

    <!-- Default configuration for other domains -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>