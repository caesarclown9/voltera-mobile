# 🔐 Безопасность Keystore - Руководство

## ⚠️ КРИТИЧЕСКИ ВАЖНО

Keystore файл и его пароли - это **единственный способ** подписи вашего Android приложения. Без них вы **НЕ СМОЖЕТЕ**:

- Обновлять приложение в Google Play Store
- Выпускать новые версии
- Восстановить доступ к приложению

**Потеря keystore = потеря приложения в Google Play!**

---

## 📋 Создание Keystore

### Использование скрипта (рекомендуется)

```powershell
# Запустить интерактивный скрипт
.\scripts\setup\create-keystore.ps1
```

Скрипт попросит вас ввести:

1. **Информацию об организации** (название, город, страна)
2. **Пароли** (скрытый ввод, минимум 6 символов)
3. **Подтверждение** параметров

### Что создаётся

После успешного выполнения создаются:

```
android/app/voltera-release.keystore          # Основной keystore
~/Documents/Voltera-Keystore-Backup/
  ├── voltera-release-YYYY-MM-DD.keystore     # Backup копия
  └── keystore-info-YYYY-MM-DD.txt            # Информация (БЕЗ паролей!)
```

---

## 🔑 Управление Паролями

### ✅ ПРАВИЛЬНО

1. **Используйте менеджер паролей**
   - 1Password
   - LastPass
   - Bitwarden
   - KeePass

2. **Сохраните в команде**
   - Shared vault в менеджере паролей
   - Зашифрованный документ
   - Сейф компании

3. **Создайте резервные копии**
   - Распечатайте и положите в сейф
   - Зашифруйте и загрузите в облако
   - Поделитесь с доверенными людьми

### ❌ НЕПРАВИЛЬНО

- ❌ Записывать в блокнот
- ❌ Хранить в email
- ❌ Сохранять в незашифрованных файлах
- ❌ Коммитить в Git
- ❌ Хардкодить в скриптах
- ❌ Отправлять в мессенджерах

---

## 🛡️ Безопасность Хранения

### Keystore файл

```bash
# ✅ Keystore УЖЕ добавлен в .gitignore
# Проверьте:
cat .gitignore | grep keystore

# Должно быть:
*.keystore
*.jks
```

### Backup локации

1. **Локальный backup** (автоматический)

   ```
   ~/Documents/Voltera-Keystore-Backup/
   ```

2. **Облачный backup** (ручной, рекомендуется)
   - Google Drive (зашифрованная папка)
   - Dropbox (с паролем)
   - OneDrive (приватная папка)
   - Собственный сервер (encrypted)

3. **Физический backup** (критичный!)
   - USB-флешка в сейфе
   - Распечатанные SHA fingerprints
   - Оффлайн хранилище

---

## 🔨 Сборка Release APK

### 1. Установка переменных окружения

**PowerShell:**

```powershell
# Установить для текущей сессии
$env:KEYSTORE_FILE = "voltera-release.keystore"
$env:KEYSTORE_PASSWORD = "ваш_пароль"
$env:KEY_ALIAS = "voltera"
$env:KEY_PASSWORD = "ваш_пароль_ключа"

# Проверить
echo $env:KEYSTORE_FILE
```

**Постоянные переменные (Windows):**

1. Win + R → `sysdm.cpl`
2. Advanced → Environment Variables
3. User variables → New:
   - `KEYSTORE_FILE` = `voltera-release.keystore`
   - `KEYSTORE_PASSWORD` = `ваш_пароль`
   - `KEY_ALIAS` = `voltera`
   - `KEY_PASSWORD` = `ваш_пароль_ключа`

### 2. Сборка APK

```powershell
# Перейти в папку android
cd android

# Собрать release APK
.\gradlew assembleRelease

# Если успешно, APK будет здесь:
# app/build/outputs/apk/release/app-release.apk
```

### 3. Проверка подписи

```powershell
# Проверить что APK правильно подписан
keytool -printcert -jarfile app\build\outputs\apk\release\app-release.apk

# Должно совпадать с SHA fingerprints из keystore-info.txt
```

---

## 🚨 Что Делать Если...

### Забыли пароль

**Плохие новости:** Пароль keystore невозможно восстановить.

**Варианты:**

1. Поискать в менеджере паролей
2. Проверить backup документы
3. Спросить у команды
4. Если не нашли - придётся создавать новый keystore и **новое приложение** в Google Play

### Потеряли keystore файл

**Решение:**

1. Найти backup в `~/Documents/Voltera-Keystore-Backup/`
2. Проверить облачное хранилище
3. Восстановить с USB-флешки
4. Если не нашли - см. "Забыли пароль"

### Keystore скомпрометирован

**Немедленные действия:**

1. Создать новый keystore
2. Обновить приложение со старым keystore (последний раз)
3. Подготовить новое приложение с новым keystore
4. Уведомить пользователей о миграции

---

## 📊 Проверочный Чеклист

Перед сборкой release проверьте:

```
□ Keystore файл существует в android/app/
□ Пароли известны и сохранены в менеджере паролей
□ Backup keystore существует в безопасном месте
□ Environment variables установлены
□ SHA fingerprints совпадают с Google Play Console
□ .gitignore включает *.keystore
□ Тесты проходят (npm run test)
□ Build проходит без ошибок
```

---

## 🔄 CI/CD Integration

### GitHub Actions

```yaml
# .github/workflows/android-release.yml
env:
  KEYSTORE_FILE: ${{ secrets.KEYSTORE_FILE }}
  KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
  KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
  KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}

steps:
  - name: Decode Keystore
    run: |
      echo ${{ secrets.KEYSTORE_BASE64 }} | base64 --decode > android/app/voltera-release.keystore

  - name: Build Release APK
    run: |
      cd android
      ./gradlew assembleRelease
```

**Настройка секретов:**

1. GitHub → Settings → Secrets and variables → Actions
2. New repository secret:
   - `KEYSTORE_BASE64` (base64 encoded keystore file)
   - `KEYSTORE_PASSWORD`
   - `KEY_ALIAS`
   - `KEY_PASSWORD`

**Encoding keystore:**

```powershell
# PowerShell
[Convert]::ToBase64String([IO.File]::ReadAllBytes("android/app/voltera-release.keystore"))
```

---

## 📚 Дополнительные Ресурсы

- [Android: Sign your app](https://developer.android.com/studio/publish/app-signing)
- [Google Play: App signing](https://support.google.com/googleplay/android-developer/answer/9842756)
- [Keystore best practices](https://developer.android.com/training/articles/keystore)

---

## 📞 Получить Помощь

Если возникли проблемы:

1. Проверьте все шаги в этом документе
2. Запустите `scripts/setup/check-environment.ps1`
3. Проверьте логи Gradle
4. Обратитесь к команде разработки

---

**Последнее обновление:** 2025-11-04
**Версия:** 1.0
**Проект:** Voltera Mobile
